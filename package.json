{
	"name": "longhai-podscript",
	"version": "0.7.1",
	"description": "Fetch podcast transcripts inside Obsidian and turn them into structured notes.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"obsidian",
		"podcast",
		"transcript"
	],
	"author": "龙海",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^22.15.17",
		"builtin-modules": "^3.3.0",
		"esbuild": "0.25.5",
		"obsidian": "latest",
		"tslib": "^2.8.1",
		"typescript": "^5.8.3"
	}
}
